name: openEuler high priority mirror periodic job

on:
  workflow_dispatch:
  pull_request:
    # Runs at every pull requests submitted in master branch
    branches: [ master ]
    paths:
    - '.github/workflows/**'
  schedule:
    # Runs every 2 hours, starting at 01:00.
    - cron:  '0 1/2 * * *'

jobs:
  mirror:
    runs-on: ubuntu-latest
    env:
      PIP_BREAK_SYSTEM_PACKAGES: "1"

    steps:
    - name: Checkout sync-config
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

    - name: Mirror the gitcode/openeuler org repos to github/openeuler-mirror.
      id: mirror
      continue-on-error: true
      uses: Yikun/hub-mirror-action@master
      with:
        src: gitcode/openeuler
        dst: github/openeuler-mirror
        dst_key: ${{ secrets.SYNC_EULER_PRIVATE_KEY }}
        dst_token: ${{ secrets.SYNC_EULER_TOKEN }}
        account_type: org
        clone_style: ssh
        force_update: true
        debug: true
        static_list: "stratovirt"
        lfs: true

    - name: Set up Python
      uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
      with:
        python-version: '3.x'

    - name: Check sync status
      id: status
      run: |
        pip install requests
        python scripts/check_sync_status.py \
          --src gitcode/openeuler \
          --dst github/openeuler-mirror \
          --dst-token "${{ secrets.SYNC_EULER_TOKEN }}" \
          --account-type org \
          --static-list "stratovirt" \
          --workflow-name "high-priority" \
          --output mirror-results.json

    - name: Upload mirror results
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
      with:
        name: mirror-results-high-priority
        path: mirror-results.json
        retention-days: 30
